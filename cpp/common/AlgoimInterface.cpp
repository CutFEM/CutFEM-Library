#include "AlgoimInterface.hpp"

// template <typename L>
// bool AlgoimInterface<MeshQuad2, L>::isCutFace(int k, int ifac) const {
//     assert(0);
//     //    int i1 = Element::nvedge[ifac][0];
//     //    int i2 = Element::nvedge[ifac][1];
//     //    if (phi(this->backMesh->operator[](k).at(i1)) * phi(this->backMesh->operator[](k).at(i2)) < 0)
//     //       return true;
//     return false;
// }